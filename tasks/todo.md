# Current Task Checklist

- [x] Resize ORIGA conversion pipeline outputs to 448x448 images
- [x] Ensure suffix bounding boxes are regenerated from resized masks
- [x] Unfreeze PaliGemma multimodal projector during fine-tuning
- [x] Increase training recipe to 20 epochs at 5e-5 LR
- [x] Rebuild ORIGA dataset files with 448 conversion settings
- [ ] Run fine-tuning again and validate mIoU trend (blocked: gated HF repo auth required)

- [x] Implement LangGraph state machine for routing and synthesis
- [x] Add Ollama tool adapters for `medgemma:1.5b` and `funcgemma`
- [x] Add latest fine-tuned LoRA model tool adapter for PaliGemma
- [x] Add CLI runner to execute the full agent flow
- [ ] Validate end-to-end run against a sample image with local Ollama service

- [x] Define V1 architecture for doctor fundus assistant in `plan.md`
- [ ] Create FastAPI backend skeleton (`app/main.py`, `app/schemas.py`)
- [x] Extract PaliGemma inference logic into reusable tool adapter
- [x] Implement MedGemma tool adapter with unified output schema
- [x] Implement router + synthesizer for tool-calling agent
- [ ] Build simple doctor UI for image upload + query + result view
- [ ] Add safety disclaimer, confidence output, and tool trace logging
- [ ] Add tests for router logic and one end-to-end API flow
- [x] Add validation mIoU computation to `train.py` and print after each epoch
- [ ] Run training and confirm epoch-over-epoch mIoU improvement
- [x] Strengthen MedGemma/FuncGemma safety prompts and remove hardcoded disclaimer suffix from final answer output
- [x] Add deterministic CDR calculator tool and model-based tool planner for dynamic tool selection
- [x] Switch final answer synthesis to MedGemma and enforce research-only / not-for-patient-use prompt policy
- [x] Remove disclaimer field and disclaimer-style prompt requirements for cleaner POC output
- [x] Add explicit DR query routing to paligemma and fallback model chain with medgemma3 for planning/synthesis
- [x] Remove medgemma3 fallback, remove DR-specific hard routing, and refine tool definitions for optic disc/cup + glaucoma workflow
- [x] Fix MedGemma VQA image payload handling for Ollama by sending file path first, then bytes retry fallback

## Review

- Planning complete for multimodel doctor assistant with tool-routing and synthesis.
- Next milestone is backend scaffolding, then model adapters and orchestration.
- LangGraph orchestration and tool adapters are now in place for local execution via `run_agent.py`.
- Training now computes validation mIoU from generated detection suffixes at each epoch and logs `eval_miou`.
- Agent outputs now rely on model-guided safety language and sanitization, without appending the previous fixed disclaimer sentence verbatim.
- Agent now includes a deterministic CDR tool and model-selected tool planning with fallback routing.
- Final narrative answer is now generated by MedGemma (not FuncGemma), with prompts aligned to research-only non-patient-use constraints.
- API output now omits the `disclaimer` field and final synthesis prompts no longer request caution/disclaimer text.
- DR/retinopathy queries now route to paligemma first; planner/synthesis now use fallback model chain that can resort to `medgemma3`.
- Planner and prompts now define paligemma as optic disc/cup segmentation for glaucoma-oriented structural questions, with medgemma as fundus interpretation model; medgemma3 fallback removed.
- MedGemma VQA now sends Ollama-native image payloads (path, then bytes fallback) to avoid missing-image-input 500 errors.
